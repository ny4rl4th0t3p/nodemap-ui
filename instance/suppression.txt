# Opt-out list for this instance: one salted SHA-256 hash of a node id per line.
# Entries come from `nodemap -hash` after a proof has passed `tools/verify`.
# The salt is never committed. Nothing here identifies a node.